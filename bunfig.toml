
[serve.static]
plugins = ["bun-plugin-tailwind"]
env = "BUN_PUBLIC_*"

[test]
# Corre una sola vez al final de toda la suite (no por archivo) y deja la
# BD de dev en el estado limpio de db/reset-and-seed.sql. Ver tests/globalTeardown.ts.
preload = ["./tests/globalTeardown.ts"]